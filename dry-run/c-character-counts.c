#include <stdio.h>
#include <stdlib.h>
int main (void){
  system("clear");
  char w_ch;
  scanf("%c", &w_ch);
  system("clear");
  for (int r=0; r=3; r++){
  for(int i=97; i<=w_ch; i++){
      printf("%c\t", i);
    }
  }
  exit(0);
}
